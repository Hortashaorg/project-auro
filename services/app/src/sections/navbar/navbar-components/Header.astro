---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"header"> {}
---

<header
  class={`flex w-full items-center justify-between mb-5 bg-primary-050 ${Astro.props.class}`}
  x-data="{ name:'' }"
  {...Astro.props}
>
  <slot />
</header>
