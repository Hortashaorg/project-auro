---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"div"> {}
---

<div
  class={`grid grid-cols-[repeat(auto-fill,minmax(12rem,1fr))] gap-5 max-w-full justify-stretch justify-items-center ${Astro.props.class}`}
  {...Astro.props}
>
  <slot />
</div>
